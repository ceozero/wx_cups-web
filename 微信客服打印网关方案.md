# 微信客服打印网关方案（wx_cups-web）

## 1. 决策

`wx_cups-web` 使用企业微信「微信客服」作为唯一消息入口，面向个人微信用户接收文字、图片和文件并提交打印。

不使用企业微信智能机器人、不维持 WebSocket 长连接，也不改造现有 `cups-web`。网关只使用专属普通帐号登录 cups-web 并调用 `/api/login`、`/api/print`。

```text
个人微信用户
  │ 微信客服链接 / 二维码
  ▼
企业微信微信客服
  │ 加密 HTTPS 回调 + kf/sync_msg
  ▼
wx_cups-web
  ├─ external_userid 白名单、下载、格式校验、去重、限流
  ├─ SQLite 消息状态与客服游标
  └─ cups-web Cookie / CSRF 会话
  ▼
现有 cups-web → CUPS / IPP → 实体打印机
```

微信客服支持在个人微信中接入，`kf/sync_msg` 支持文本、图片、文件等消息；客服回执使用 `kf/send_msg`。每次客户主动发送消息后的 48 小时内，客服 API 最多可向该用户下发 5 条消息。

`sync_msg` 同时可能包含系统事件和接待人员消息，网关只处理 `origin=3` 的微信客户入站消息。每次客户消息仅回复最终处理结果，以节省客服会话的回复额度。

## 2. 企业微信配置

1. 在企业微信管理后台开通「微信客服」，创建客服帐号。
2. 在「微信客服应用」开启 API，授权需要使用的客服帐号由 API 管理。
3. 记录 `CorpID`、微信客服 Secret、客服帐号 `open_kfid`。
4. 配置回调 URL、Token、EncodingAESKey。回调 URL 必须是企业微信可从公网访问的 HTTPS 地址。
5. 使用客服链接或二维码让个人微信用户进入会话。

网关接到 `kf_msg_or_event` 回调后，验证签名、AES 解密，使用回调携带的 Token 调用 `kf/sync_msg` 拉取真实消息。回调会立即响应，耗时的下载与打印异步执行，避免企业微信重试。

## 3. 环境变量

| 变量 | 用途 |
| --- | --- |
| `CUPS_WEB_URL` | cups-web 地址，host 网络默认 `http://127.0.0.1:1180` |
| `CUPS_WEB_USER` / `CUPS_WEB_PASSWORD` | cups-web 专用普通用户凭据 |
| `PRINTER_URI` | 固定的 CUPS 队列 URI |
| `WECOM_CORP_ID` | 企业微信 CorpID |
| `WECOM_KF_SECRET` | 微信客服 Secret |
| `WECOM_CALLBACK_TOKEN` | 回调签名 Token |
| `WECOM_CALLBACK_ENCODING_AES_KEY` | 企业微信生成的 43 位回调密钥 |
| `WECOM_OPEN_KF_IDS` | 允许处理的逗号分隔 `open_kfid` 白名单 |
| `WECOM_ALLOWED_EXTERNAL_USERS` | 允许打印的逗号分隔个人微信 `external_userid` 白名单 |
| `WECOM_CALLBACK_PORT` | 本地回调监听端口，默认 `3000` |
| `PRINT_CONFIRMATION_TTL_MS` | 用户确认菜单有效期，默认 10 分钟 |
| `PRINT_STATUS_POLL_MS` | CUPS 状态查询间隔，默认 5 秒 |
| `PRINT_STATUS_TIMEOUT_MS` | CUPS 状态最长查询时间，默认 10 分钟 |

用户首次发送消息时，网关会以 `wecom_kf_ignored_sender` 事件记录其 `external_userid`，管理员审核后加入白名单并重启服务。

## 4. 打印与可靠性

| 输入 | 动作 |
| --- | --- |
| 文本 | 用户确认后生成 UTF-8 `.txt` 并交给 cups-web |
| 图片 | 用户确认后以 `media_id` 下载、校验并交给 cups-web |
| 文件 | 用户确认后以 `media_id` 下载，检查扩展名、MIME、文件头后交给 cups-web |
| 其他类型 | 回复不支持，不提交打印 |

SQLite 以客服 `msgid` 为唯一键，状态为：

```text
received → downloading → validating → submitting → accepted | rejected | uncertain
```

- 重复投递直接返回原结果，绝不再打印。
- 用户发送后先收到“确认打印 / 取消”菜单；未确认内容不会下载或提交。
- cups-web 成功返回后保存 CUPS job id 和页数，并轮询 CUPS 的 IPP `job-state`。
- CUPS 状态为 `completed` 后向客户发送一次完成回执；该回执表示 CUPS 已完成任务，实际出纸仍以打印机为准。
- 上传连接中断、超时或网关在提交时重启，一律记录 `uncertain`，不自动重发。
- 客服回复失败只记录错误，不会导致网关进程退出。

## 5. 部署

`docker-compose.yml` 只运行 wx_cups-web，使用 host 网络，并绑定 `./data:/app/data`。cups-web 由独立 Compose 运行。

反向代理示意：

```nginx
location = /wecom/kf/callback {
  proxy_pass http://127.0.0.1:3000;
  proxy_set_header Host $host;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

反向代理必须提供有效 HTTPS 证书；不要将 `WECOM_KF_SECRET`、cups-web 密码或回调密钥提交到 Git、镜像或日志。

## 6. 验收

1. 回调 URL 验证成功，网关出现 `wecom_callback_listening` 日志。
2. 个人微信用户发送文本、图片、PDF 与受支持 Office 文件，各得到客服回执和 CUPS 作业。
3. 同一客服消息重复投递不重复出纸；重启后仍识别历史消息。
4. cups-web 不可用时返回失败或状态未知，且不自动重发。
5. 未加入 `WECOM_ALLOWED_EXTERNAL_USERS` 的个人微信用户无法打印。
