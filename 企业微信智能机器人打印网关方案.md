# 企业微信智能机器人打印网关方案（不改 cups-web）

## 1. 决策

**不对 cups-web https://github.com/hanxi/cups-web 做任何二开，不构建自定义 cups-web 镜像。**

新增独立的 Node.js + TypeScript 打印网关 `wx_cups-web`。网关使用一个专属、非管理员的 cups-web 用户，通过现有的登录、Cookie、CSRF 与打印接口调用 `cups-web`：

```text
企业微信成员
  │  单聊发送：文字 / 图片 / 文件
  ▼
企业微信智能机器人（WSS）
  ▼
wx_cups-web（新增）
  ├─ 用户白名单、下载/解密、格式校验、去重、限流
  ├─ 保存 cups-web 登录会话与 CSRF Token
  ▼
现有 cups-web
  POST /api/login
  POST /api/print
  ▼
现有 CUPS / IPP → 实体打印机
```

现有 cups-web 已能处理 PDF、图片、文本、Office 与 OFD，并负责文件保存、页数计算、打印记录和 IPP 投递；网关不重复实现这些能力。

## 2. 为什么不是直连 CUPS

网关直接调用 CUPS 虽然也无需修改 cups-web，但会绕过 cups-web 的 Office/OFD/图片/文本转换、网页可见的打印记录、文件保留策略和已有打印参数逻辑。因此 v1 直接调用现有 cups-web API。

现有 `/api/print` 需要有效 Session、`csrf_token` Cookie 和同值 `X-CSRF-Token` 请求头；网关必须作为正常 API 客户端登录，不能跳过认证。

## 3. cups-web 前置配置（一次性人工操作）

1. 使用管理员登录 cups-web，立即修改默认管理员密码。
2. 新建普通用户 `wecom-gateway`，角色为 `user`，设置随机高强度密码；它没有用户、驱动、设置等管理员权限。
3. 在 CUPS 中创建并验证打印队列，例如 `Office_A4`，确认处于共享/可发现状态。
4. 在网关配置中固定该队列的 URI；用户输入不能改变打印机、份数、彩色、单双面等 v1 默认参数。

建议默认策略：A4、黑白、单面、1 份、自动缩放。打印机 URI 只存在于网关配置，不通过机器人指令接收。

## 4. 网关与现有 API 的精确交互

### 4.1 登录与会话管理

网关启动时、或发现会话失效时：

```http
POST http://cups:8080/api/login
Content-Type: application/json

{"username":"wecom-gateway","password":"<secret>"}
```

成功响应会设置 Session Cookie 和 `csrf_token` Cookie。网关使用支持 Cookie Jar 的 HTTP 客户端保存两者；提交打印时带上 Cookie，并把 `csrf_token` 的值同时放进 `X-CSRF-Token`。

会话刷新只能单飞执行，避免并发消息同时登录触发 cups-web 的登录失败限流。收到 `401` 或 `403` 时，网关重新登录一次；随后仅对**尚未发出**的请求继续处理，绝不对已有上传超时的打印请求盲目重发。

### 4.2 现有打印接口调用

```http
POST http://cups:8080/api/print
Cookie: <session>; csrf_token=<token>
X-CSRF-Token: <token>
Content-Type: multipart/form-data

file=<二进制>
printer=http://cups:631/printers/Office_A4
duplex=false
color=false
copies=1
paper_size=A4
print_scaling=auto
```

成功时读取现有响应的 `jobId`、`pages`、`ok` 并回复机器人。打印历史会归属 `wecom-gateway` 这个 cups-web 用户。

## 5. 机器人消息处理规则

| 用户输入 | 网关动作 | 结果 |
|---|---|---|
| 文本 | 生成 UTF-8 `.txt` 文件 | 由 cups-web 转 PDF 后打印 |
| 图片 | 立即下载、使用消息携带的 `aeskey` 解密、校验格式 | 交给 cups-web 图片管线 |
| 文件 | 立即下载解密，校验文件头、MIME、扩展名 | 交给 cups-web 转换/打印 |
| 图文混排 | 拆成文字和各图片，按原顺序分别提交 | 多个打印任务 |
| 语音/视频/压缩包/未知格式 | 不提交 | 机器人回复不支持原因 |

网关收到消息后先回复“已接收，正在提交”。cups-web 返回成功后再回复“已提交 CUPS 任务 #…，共 N 页”。这个状态只代表 CUPS 接受作业，并不代表纸张已经出纸；不改 cups-web 的前提下，v1 不承诺最终出纸状态回执。

## 6. 去重与可靠性边界

网关使用自己的 SQLite 持久卷，以企业微信 `msgid` 建唯一索引，状态为：

```text
received → downloading → validating → submitting → accepted | rejected | uncertain | failed
```

- 同一个 `msgid` 再次送达：直接返回原有结果，绝不再次打印。
- cups-web 返回成功：状态为 `accepted`，保存 job id 和页数。
- 上传请求出现网络超时或连接中断：状态为 `uncertain`，回复“提交状态未知，请查看打印机或管理后台”；**不自动重发**。

这是不改 cups-web 时必要的“宁可少打、不重复出纸”策略。现有 `/api/print` 没有外部消息 ID/幂等键，网关无法在网络中断后确认请求是否已被 cups-web/CUPS 接受；因此无法同时保证“绝不重复”和“绝不漏打印”。如未来业务必须实现端到端严格幂等，才需要 cups-web 增加幂等键接口或查询能力。

## 7. 安全策略

- 企业微信机器人可见范围限制为授权员工；网关再做 `userid` 白名单。
- 仅接受白名单格式：PDF、JPG/JPEG/PNG/GIF/HEIC、DOC/DOCX、XLS/XLSX、PPT/PPTX、OFD、TXT/MD/HTML。
- 默认上限：单文件 20 MB、单任务 20 页、每人 10 分钟 10 个任务；超过直接拒绝。
- 以文件头、MIME、扩展名三重校验；净化文件名，禁止路径片段和控制字符。
- `WECOM_BOT_SECRET` 与 cups-web 专用用户密码使用 Docker Secret；禁止进入镜像、Git、日志或机器人回复。
- 网关只调用 cups-web 的 `/api/login` 和 `/api/print`；专用账号必须是普通用户。
- cups-web 继续按其原有保留策略清理上传文件；网关媒体仅在内存中下载、解密和提交，不产生待清理的临时文件。

## 8. Docker Compose 部署

不替换官方 cups-web 镜像：

```yaml
services:
  cups:
    image: hanxi/cups-web:latest
    # 延用项目官方的 USB、数据卷、CUPS 配置和驱动挂载
    networks:
      print_backplane:
        aliases: [cups]
      printer_lan: {}
    # 仅在需要 Web 管理后台时映射 1180:8080；不对公网公开

  wx_cups-web:
    build: ./wx_cups-web
    environment:
      CUPS_WEB_URL: http://cups:8080
      CUPS_WEB_USER: wecom-gateway
      CUPS_WEB_PASSWORD_FILE: /run/secrets/cups_web_gateway_password
      PRINTER_URI: http://cups:631/printers/Office_A4
      WECOM_BOT_ID: ${WECOM_BOT_ID}
      WECOM_BOT_SECRET_FILE: /run/secrets/wecom_bot_secret
    secrets:
      - cups_web_gateway_password
      - wecom_bot_secret
    networks: [bot_egress, print_backplane]
    volumes:
      - gateway-data:/app/data
    restart: unless-stopped

networks:
  print_backplane:
    internal: true
  bot_egress: {}
  printer_lan: {}

secrets:
  cups_web_gateway_password:
    file: ./secrets/cups_web_gateway_password.txt
  wecom_bot_secret:
    file: ./secrets/wecom_bot_secret.txt

volumes:
  gateway-data:
```

`wx_cups-web` 同时连接 `bot_egress`（到企业微信的 WSS 出站连接）和 `print_backplane`（只访问 cups-web）。`cups` 同时连接 `print_backplane` 与 `printer_lan`（访问局域网打印机）。宿主机不发布网关端口，也不需要为机器人配置公网回调。

## 9. 网关技术选型

网关使用 **Node.js 22 LTS + TypeScript**：

- 使用企业微信团队维护的 `@wecom/aibot-node-sdk` 接收 `text`、`image`、`file` 与 `mixed` 消息，维持 WebSocket、下载并解密媒体、回发消息。
- 使用 `axios` + `tough-cookie` + `axios-cookiejar-support` 维护 cups-web 的登录 Cookie 与 CSRF Token。
- 使用 `form-data` 或 Node 原生 `FormData` 提交流式 multipart 文件。
- 使用 `better-sqlite3` 或 SQLite 驱动持久化消息去重与审计状态。

选 Node 的原因是企业微信官方团队已提供 Node SDK；用 Go 需要自己实现或依赖非官方协议 SDK。cups-web 不二开时，网关是否与 cups-web 同语言没有代码复用收益，因此 Node 是风险更低、落地更快的选择。

## 10. 实施与验收

1. 创建机器人和专用 cups-web 普通用户；配置默认打印机。
2. 完成文本链路：白名单用户发文字 → `.txt` → `/api/print` → CUPS job id 回执。
3. 接入图片和文件下载/解密，加入格式、大小、页数校验。
4. 加入 SQLite 去重、超时 `uncertain` 状态、限流和结构化日志。
5. 在 Docker 中完成重启/断网/CUPS 离线/重复消息/错误格式等验收。

验收通过标准：支持格式各打印一次；同一消息重复投递不会重复出纸；网关重启后仍能识别已处理消息；CUPS 不可用时给出失败或状态未知回执；机器人密码和 cups-web 密码不出现在日志或镜像中。
