name: wx_cups-web

services:
  wx_cups-web:
    build: ./wx_cups-web
    container_name: wx_cups-web
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
